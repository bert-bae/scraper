Global:
  BaseUrl: 'https://www.otisamantha.com'
  Options:
    # CrawlerIO options go here...

# Local options will always overwrite global options if two of the same values exist
Resources:
  Otisamantha:
    Url: false
    Path: /
    Output: true
    Instructions:
      - Selector: 'p'
        Output: post
        Operation: text
